<%= form_with(model: goal_trigger, local: true) do |form| %>
  <% if goal_trigger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(goal_trigger.errors.count, "error") %> prohibited this goal_trigger from being saved:</h2>

      <ul>
      <% goal_trigger.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :frequency %>
    <%= form.text_field :frequency, id: :goal_trigger_frequency %>
  </div>

  <div class="field">
    <%= form.label :metric_id %>
    <%= form.text_field :metric_id, id: :goal_trigger_metric_id %>
  </div>

  <div class="field">
    <%= form.label :trigger_level %>
    <%= form.number_field :trigger_level, id: :goal_trigger_trigger_level %>
  </div>

  <div class="field">
    <%= form.label :spoils %>
    <%= form.number_field :spoils, id: :goal_trigger_spoils %>
  </div>

  <div class="field">
    <%= form.label :fee %>
    <%= form.number_field :fee, id: :goal_trigger_fee %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_area :message, id: :goal_trigger_message %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
